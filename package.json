{
  "name": "chat-gpt-answer-notifier",
  "private": true,
  "scripts": {
    "build": "run-p build:*",
    "build:ts": "esbuild src/content-script.ts --bundle --outfile=dist/content-script.js",
    "build:copy": "cpx manifest.json dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "cpx": "^1.5.0",
    "esbuild": "^0.19.1",
    "npm-run-all": "^4.1.5",
    "prettier": "^3.0.1"
  },
  "volta": {
    "node": "18.12.1"
  }
}
